{
  "name": "bedlog-db-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Local Postgres backup service for a BedLog kiosk device — mirrors localStorage state, never a primary data source.",
  "main": "server.cjs",
  "scripts": {
    "start": "node server.cjs"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
